function calcularAreaQuadrado(lado) {
    return lado * lado;
}

// Exemplo de uso
let lado = 4;
let area = calcularAreaQuadrado(lado);
console.log(`A área de um quadrado com lado de comprimento ${lado} é ${area}`);
